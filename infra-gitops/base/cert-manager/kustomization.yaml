apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Only the issuer chain is GitOps-managed here. cert-manager itself (CRDs +
# controller) is helm-installed out-of-band, same pattern as nginx-ingress /
# kube-prometheus / gatekeeper. See README.md.
resources:
  - cluster-issuer.yaml
