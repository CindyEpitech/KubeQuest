apiVersion: v1
kind: Pod
metadata:
  name: opa-bad-latest
  namespace: default
spec:
  containers:
    - name: nginx
      image: nginx:latest
      resources:
        requests:
          cpu: "10m"
          memory: "16Mi"
        limits:
          cpu: "50m"
          memory: "64Mi"
