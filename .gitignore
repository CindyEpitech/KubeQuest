.env
.env.*
!.env.example

# Local plaintext secrets dump — never commit
documentation/CREDENTIALS.md

# HomePedia app lives in its own repo (CindyEpitech/HomePedia).
# A local copy may sit here for convenience, but it is built from its own repo
# (see scripts/deploy-homepedia.sh) — never commit it into KubeQuest.
/homepedia/
